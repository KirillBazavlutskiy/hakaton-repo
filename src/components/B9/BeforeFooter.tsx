import s from './BeforeFooter.module.scss'

const BeforeFooter = () => {
    return (
        <div className={s.container}>
            <h1>Template for official aid request for organisations</h1>
            <button>Fill the form</button>
        </div>
    );
};

export default BeforeFooter;